{% block content %}
	{% for content in entry.artBlogImages.all() %}
		<div class="artwork-blog">
			{% include "/_siteContent/" ~ content.type %}

			<p class="artworkBlog-text">{{ entry.title }}</p>
		</div>
	{% endfor %}

{% endblock content %}
